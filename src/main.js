var typed = new Typed(".typed-text", {
  strings: ["About MSe:"],  // The text to type out
  typeSpeed: 50,               // Speed at which the text is typed
  backSpeed: 0,                // No speed for backspacing

  startDelay: 500,             // Delay before typing starts
  loop: true,                 // Do not loop the animation
  showCursor: true             // Show the cursor (set to false if you don't want it)
});
